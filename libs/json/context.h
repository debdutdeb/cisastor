#pragma once

// While walking, parser sets up state, these are
// Expectations -> transformations
// E.g., expectation -> closing '"', transformation -> new token in the ast

#include "containers/iterator.h"

struct context {
};

struct transformer_queue {
};


// rule
